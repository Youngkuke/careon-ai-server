# Phase 6: 매칭 후 보완질문 (확인_불가 해소 라운드)

## 왜 이 phase가 필요한가

매칭 프롬프트의 대전제는 "추천 가능한 건 다 추천하되, 확실하지 않은
건 절대 추천하지 않는다"는 것이다. 이 원칙을 지키려면 두 가지가
동시에 이뤄져야 한다:

1. 정보가 없어서 판단을 못 한 제도("확인_불가")를 "적합"으로 착각해서
   보여주면 안 된다 (거짓 추천의 위험).
2. 반대로, 정보 하나만 더 있으면 확실히 추천 가능한 제도를 그냥
   "확인_불가"로 방치하고 넘어가면, 사용자가 실제로 받을 수 있는
   혜택을 놓치게 된다 (기회 손실의 위험).

phase 6은 이 두 위험 사이의 간극을 메우는 단계다. 매칭 프롬프트가
"확인_불가"로 분류한 제도들을 보고, **그 제도들 각각에 필요한
정보를 모아서 한 번에 되물어본다.** 이때 질문은 공통 필드
(phase 1~4처럼 모든 사용자에게 똑같이 묻는 것)가 아니라, **그
순간 애매해진 특정 제도 하나를 위한 맞춤 질문**이어도 된다.
오히려 그래야 한다 — "이 제도 받으려면 이거 하나만 더 알면 돼요"
라는 구체적 동기가 있는 질문이, 목적을 알 수 없는 일반 질문보다
훨씬 자연스럽고 답변률도 높다.

## 입력
- 매칭 프롬프트의 "확인_불가" 리스트 (institution_id, missing_field,
  reason 포함)
- 현재까지의 유저 프로필

## 질문 생성 규칙

1. "확인_불가" 리스트에 있는 제도 수만큼 질문이 나올 수도 있다.
   하나로 퉁치려 하지 않는다.
   → 이유: 예를 들어 제도 A는 보증금이 궁금하고 제도 B는 재직
     여부가 궁금하다면, 이 둘은 서로 다른 질문이 되는 게 맞다.
     억지로 공통 질문으로 뭉치면 특정 제도에 필요한 정보가
     제대로 안 걸러진다.

2. 다만 여러 질문을 한꺼번에 쏟아붓지 않는다. 자연스러운 대화
   순서로 묶어서, 관련 있는 것끼리 이어 붙인다 (예: 보증금과
   월세는 같이 물어봐도 자연스러움).
   → 이유: phase 1~4에서 지켜온 "한 번에 최대 2개 질문" 원칙이
     여기서도 유효하다. 확인_불가 제도가 5개라고 질문을 5개
     따로따로 던지면 사용자가 질려한다.

3. 각 질문 앞에 "이걸 알면 어떤 제도를 확인해줄 수 있는지" 짧게
   언급한다.
   예: "혹시 지금 사시는 곳 보증금이랑 월세 아시면 알려주세요.
   이거 알면 월세지원 관련해서 하나 더 확인해드릴 수 있어요."
   → 이유: 목적 없이 묻는 질문보다, 구체적 이득이 걸린 질문이
     심리적 저항이 적다. phase 4에서 "왜 병역을 묻는지" 이유를
     밝혔던 것과 같은 논리다.

4. 이 라운드는 딱 한 번만 진행한다. 사용자가 "모르겠다"고 답한
   항목은 그 제도를 그대로 "확인_불가"로 최종 결과에 남기고,
   같은 걸 다시 캐묻지 않는다.
   → 이유: 무한히 되묻는 루프는 사용자 피로를 유발한다. 그리고
     "모른다"는 이미 여러 페르소나에서 확인된 정상적인 답변
     패턴이라, 이를 실패로 취급하고 계속 압박하면 안 된다.

5. 이 라운드에서 얻은 답변은 원래 필드(housing_deposit 등)에
   그대로 반영하고, income_value_status 같은 상태값도 함께
   갱신한다 (예: 'unknown_needs_probe' → 'known').
   → 이유: 여기서 얻은 정보도 결국 user_profile의 정식 데이터가
     되어야 다음 재방문 때 또 물어보지 않는다.

## 대화 원칙

1. "확인_불가"였던 제도명을 직접 언급하지 않는다 (예: "OO제도
   때문에 여쭤보는 건데"라고 하지 않는다). 대신 "이런 종류의
   지원을 확인하려면"처럼 완곡하게 표현한다.
   → 이유: 아직 확정 안 된 제도명을 미리 언급하면, 나중에 결국
     그 제도가 부적합으로 나왔을 때 사용자가 기대했다가 실망하는
     낙차가 커진다. 신중하게, 결과가 나온 후에만 제도명을 확정
     언급한다.

## 질문 예시
- "거의 다 됐어요! 몇 가지만 더 확인하면 딱 맞는 제도를 더 정확히
   찾아드릴 수 있을 것 같아요. 혹시 지금 사시는 곳 보증금이랑
   월세가 어느 정도인지 아세요? 이거 알면 주거 관련해서 하나 더
   확인해드릴 수 있어요."
- "그리고 혹시 지금 어디서 일하고 계신지, 재직 중이신지도 알려주시면
   좋을 것 같아요. 이것도 조건 중 하나라서요."
- (모른다고 답할 시) "아, 그러시면 어쩔 수 없죠. 이 부분은 나중에
   직접 확인해보실 수 있게 안내해드릴게요."

## 추출 규칙
{
  "housing_deposit": 80000000,
  "housing_monthly_rent": 600000,
  "financial_detail_status": "known"
}
※ 응답이 "모른다"면 해당 필드는 업데이트하지 않고
  financial_detail_status만 'unknown_needs_probe' 유지.

## 다음 단계
이 답변을 반영해서 매칭 프롬프트를 딱 한 번 재실행한다. 재실행
후에도 여전히 확인_불가로 남는 제도는, 최종 결과에 다음과 같이
표시해서 사용자에게 그대로 보여준다 (숨기지 않는다):

"이 제도는 [부족한 정보] 확인이 더 필요해서 확실히 추천드리긴
어렵지만, 조건이 맞으실 수도 있어요. [원문 링크]에서 직접
확인해보시는 것도 추천드려요."

→ 이유: "확실하지 않으면 추천하지 않는다"는 원칙은 지키되,
  그렇다고 그 제도의 존재 자체를 사용자에게 숨기는 건 다른
  문제다. "우리가 확신을 못 줄 뿐, 기회가 없는 건 아니다"라는
  걸 투명하게 전달해야, 놓치는 혜택이 최소화된다.

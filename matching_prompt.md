# 매칭 프롬프트: 유저 프로필 ↔ 제도 DB 매칭

## 왜 이런 구조로 매칭하는가

이 프롬프트는 SQL 쿼리가 아니라 LLM이 직접 판단하는 방식을 쓴다.
지금 규모(57건)에서는 제도 목록 전체를 통째로 컨텍스트에 넣고
LLM이 하나하나 대조하는 게, 벡터 검색이나 SQL 필터보다 오히려
정확하다.

이 매칭에서 반드시 지켜야 할 원칙이 하나 있다: **"부적합"과
"확인 불가"를 구분해야 한다.** 예를 들어 어떤 제도가 "임차보증금
8천만원 이하"를 조건으로 거는데, 사용자가 housing_deposit을
아직 답하지 않았다면(financial_detail_status가 'not_asked'나
'unknown_needs_probe'), 이건 "조건을 못 만족한다"가 아니라
"확인이 안 돼서 판단을 못 한다"는 뜻이다. 이 둘을 구분 안 하고
전부 '추천 안 함' 하나로 묶어버리면, 실제로는 자격이 될 수도
있는 제도를 사용자에게 영영 안 보여주는 결과가 된다.

## 입력

아래 두 가지가 주어진다.

1. 유저 프로필 (JSON, phase 1~5에서 수집된 전체 데이터 +
   care_recipients + user_income_signals + user_current_benefits)
2. 제도 목록 (57건, policies + connect_policy_policy_types +
   connect_policy_documents 조인 결과를 텍스트 블록으로 변환한 것)

## 매칭 규칙

1. 나이 비교 시, military_service_status가 '제대(연장가능)'이면
   제도의 age_max에 military_service_extension_years를
   더한 값으로 비교한다.
   → 이유: 57건 중 다수 제도가 "제대군인은 최대 3년 연장"을
     명시하는데, 이걸 반영 안 하면 실제로는 자격이 되는 제대군인
     사용자를 나이 초과로 잘못 걸러낸다.

2. 소득 비교 시, income_value_status가 'known'이나
   'estimated_from_signals'면 비교하되, 'estimated_from_signals'인
   경우 결과에 "추정치 기준"이라고 명시한다. 'unknown_needs_probe'나
   'not_asked'면 소득 조건이 있는 제도는 "확인 불가"로 분류한다.
   → 이유: 추정값으로 판단했다는 걸 숨기면, 나중에 실제 소득과
     추정치가 달라서 매칭이 틀렸을 때 왜 틀렸는지 추적할 수 없다.
     사용자에게도 "이건 대략적인 판단"이라는 걸 투명하게 알려야
     신뢰를 잃지 않는다.

3. 주거 관련 숫자 조건(보증금, 월세, 재산, 차량가액)은
   financial_detail_status가 'known'일 때만 비교하고, 그 외에는
   "확인 불가"로 분류한다. 임의로 "아마 해당될 것 같다"고
   추측하지 않는다.
   → 이유: 이 값들은 우회질문으로도 안 물어봤을 수 있는 값이라,
     안 물어본 걸 매칭 시도 자체를 안 하는 것과 "물어봤는데
     모른다"를 구분해야 한다.

4. has_basic_livelihood_support, has_cha_sang_wi 같은 값은
   진술(source='self_declared')과 계산(source='system_inferred')이
   충돌할 경우, 진술값을 우선하되 결과에 "진술과 추정이 달라 확인
   권장"이라고 표시한다.
   → 이유: 사용자 본인이 제일 잘 아는 정보(본인이 수급자인지)를
     시스템의 계산이 뒤집는 건 위험하다. 다만 그 충돌 자체는
     투명하게 드러내야 나중에 재확인할 수 있다.

5. user_current_benefits에 이미 있는 policy_id와, 그 제도의
   duplication_restriction(중복수혜불가조항)에 명시된 제도명을
   대조해서, 중복되는 제도는 추천에서 제외한다.
   → 이유: 이미 받고 있는 제도와 중복되는 걸 추천하면 실제
     신청 시 거절당해 사용자가 시간을 낭비하게 된다.

5-1. 후보 제도들끼리 서로 중복수혜불가 관계에 있어도, 그
     이유만으로 어느 한쪽을 추천에서 제외하지 않는다. 오직
     user_current_benefits에 "이미 받고 있다"고 기록된 제도와
     겹칠 때만 제외한다. 아직 둘 다 안 받은 상태라면, 설령 두
     제도가 서로 "동시 수급 불가" 관계여도 둘 다 "적합"으로
     분류해서 함께 보여준다.
     → 이유: 추천 단계와 실제 신청 단계는 다른 문제다. 사용자는
       여러 선택지를 다 보고 나서 "그럼 이 중에 뭘 신청할지"를
       스스로 정해야 하는데, 시스템이 미리 하나를 골라서 감추면
       사용자의 선택권을 빼앗는 셈이 된다. 실제 배타성은 "신청"
       시점에 검증하면 될 문제지, "추천" 시점에 걸러질 문제가
       아니다.

5-2. 다만 서로 배타적인 제도들을 함께 추천할 때는, 결과에
     "이 둘은 동시에 받을 수 없으니 하나만 선택하셔야 해요" 같은
     안내를 caution 필드에 남긴다.
     → 이유: 규칙 5-1대로 둘 다 보여주되, 사용자가 "둘 다 신청해야
       겠다"고 착각해서 나중에 한쪽이 거절당하는 걸 막기 위해,
       선택이 필요하다는 사실 자체는 알려줘야 한다.

6. care_recipients.condition_summary와 제도의 support_target/
   qualification_text를 의미적으로 비교한다 (예: "뇌병변장애 1급"과
   "장애가 있는 가족"은 같은 것으로 판단). 이건 통제어휘 매칭이
   아니라 LLM의 의미 판단 영역이다.
   → 이유: 이전에 결정한 대로, 서술적 조건은 억지로 카테고리화
     하지 않고 LLM이 자연어 그대로 이해하도록 남겨뒀다.

7. 분야가 "돌봄/의료"와 무관해 보여도(예: 자격증 응시료 지원,
   청년수당) 나이·소득·지역 등 다른 조건이 맞으면 반드시
   추천 후보에 포함한다.
   → 이유: 이 서비스는 "가족돌봄청년에게 도움이 되는 모든 서울시
     복지"를 다루기로 확정했다. 제도명에 "돌봄"이나 "의료"가
     없다고 자동으로 배제하면 안 된다.

8. 하나의 제도라도 notes 필드에 "출처 간 불일치" 표시가 있으면,
   추천 결과에도 그 불확실성을 그대로 전달한다 (예: "신청기간이
   출처마다 달라 정확한 마감일은 원문 링크에서 재확인 필요").
   → 이유: 정보 자체가 불확실한데 확정적으로 안내하면, 사용자가
     잘못된 마감일을 믿고 신청 기회를 놓칠 수 있다.

9. deadline_type이 "예산소진시마감"이거나 application_deadline이
   지난 날짜인 제도는, 부적합으로 완전히 빼지 말고 "적합" 판단은
   그대로 하되 caution에 "마감이 임박했거나 예산 소진으로 조기
   마감될 수 있음"을 표시한다.
   → 이유: 실제 자격은 충족하는데 마감 임박이라는 이유로 아예
     안 보여주면 기회 자체를 차단하게 된다. 판단은 사용자가 하게
     둔다.

10. cost(자부담금) 필드가 비어있지 않은 제도는 caution에 자부담
    금액/비율을 반드시 명시한다.
    → 이유: 이전에 확정한 요구사항. 자부담이 있는데 이를 알리지
      않으면 사용자가 전액 지원으로 착각하고 신청했다가 예상치
      못한 비용 부담에 놀랄 수 있다.

## 출력 형식

제도를 세 그룹으로 나눠서 반환한다. 이분법(맞다/틀리다)이 아니라
세 그룹인 이유는 매칭 규칙 서두에서 설명한 대로다.

```json
{
  "적합": [
    {
      "policy_id": 2,
      "name": "제도명",
      "reason": "왜 맞는지 (충족한 조건들을 구체적으로)",
      "caution": "출처 불일치, 추정치 사용, 자부담금, 마감 임박 등 주의사항 (없으면 null)"
    }
  ],
  "확인_불가": [
    {
      "policy_id": 7,
      "name": "제도명",
      "missing_field": "housing_deposit",
      "reason": "임차보증금 정보가 아직 확인되지 않아 자격 여부를 판단할 수 없음"
    }
  ],
  "부적합": [
    {
      "policy_id": 16,
      "name": "제도명",
      "reason": "대상 연령(21~23세)에 해당하지 않음 (현재 24세, 제대군인 연장 적용해도 26세까지만 해당)"
    }
  ]
}
```

## 다음 행동 지침

"확인_불가"로 분류된 제도가 있으면, 이걸 이유로 사용자에게
자연스럽게 추가 정보를 되물어볼 수 있다는 신호로 활용한다
(예: "혹시 지금 사시는 곳 보증금이나 월세 아시면, 하나 더
확인해드릴 수 있는 제도가 있어요"). 단, phase 4에서 이미 "모른다"
로 넘어간 항목이면 다시 캐묻지 않고 그냥 확인 불가 상태로 둔다.
→ 이유: 매칭 단계에서 놓친 정보가 오히려 자연스러운 추가 질문의
  계기가 될 수 있지만, 이미 한 번 거절/회피된 질문을 반복하면
  앞서 쌓은 배려가 무너진다.

# 웹 API 연결 현황

이 문서는 `api.md` 기준 웹 API 41개의 현재 프론트 연결 상태를 정리한다.

- 사용 중: 18개
- 보류: 23개

## 사용 중 API

### `POST /api/web/users/login`

- 기능: 이메일/비밀번호로 로그인하고 `access_token`, `carer_id`, `diagnosis_completed`를 받는다.
- 사용 위치: 로그인 화면.
- 연결 이유: 로그인 후 토큰 저장, 2단계 챗봇 또는 맞춤 제도 화면 진입을 결정하는 핵심 인증 API다.

### `POST /api/web/users/register`

- 기능: 신규 유저를 만들고 자동 로그인용 `access_token`을 받는다.
- 사용 위치: 회원가입 화면.
- 연결 이유: 1단계 진단 후 사용자가 결과 저장과 2단계 진단으로 넘어가기 위해 필요하다. 가입 시 관심 유형 ID도 함께 보낸다.

### `POST /api/web/users/password/reset-link`

- 기능: 비밀번호 재설정 링크를 이메일로 발송한다.
- 사용 위치: 비밀번호 찾기 화면.
- 연결 이유: 로그인하지 못하는 사용자가 계정 접근을 복구할 수 있어야 한다.

### `POST /api/web/users/password/reset`

- 기능: `reset_token`과 새 비밀번호로 비밀번호를 재설정한다.
- 사용 위치: 비밀번호 재설정 링크 진입 화면.
- 연결 이유: 이메일 링크를 받은 사용자가 웹에서 새 비밀번호를 설정할 수 있어야 한다.

### `GET /api/web/users/me`

- 기능: 로그인한 사용자의 프로필과 상태를 조회한다.
- 사용 위치: 앱 시작 시 세션 복구, 로그인/회원가입 직후, 2단계 완료 후, 마이페이지 수정 후.
- 연결 이유: 사용자 이름, 진단 완료 여부, 앱 설치 상태, 알림 상태 등 화면 분기에 필요한 기준 데이터다.

### `PATCH /api/web/users/me`

- 기능: 로그인한 사용자의 이름, 비밀번호, 거주 지역, 알림 수신 여부 등을 수정한다.
- 사용 위치: 마이페이지 내 정보 수정.
- 연결 이유: 현재 웹에서 제공하는 계정 관리 기능이다.

### `DELETE /api/web/users/me`

- 기능: 로그인한 사용자의 계정을 삭제한다.
- 사용 위치: 마이페이지 회원 탈퇴.
- 연결 이유: 계정 삭제는 사용자 계정 관리의 기본 기능이다.

### `PATCH /api/web/users/me/app-install-status`

- 기능: 웹의 앱 설치 유도 모달 응답을 저장한다.
- 사용 위치: 제도 저장 후 앱 설치 안내 모달.
- 연결 이유: 사용자가 앱 설치를 완료했는지 또는 나중에 하겠다고 했는지 저장해 모달 노출을 제어한다.

### `GET /api/web/policies/alternatives`

- 기능: 가족돌봄청년 대상이 아니거나 로그인/회원가입을 미룬 사용자에게 대안 복지 목록을 제공한다.
- 사용 위치: 1단계 진단 결과 화면.
- 연결 이유: 핵심 대상자가 아니어도 사용자가 관심 유형에 맞는 일반 복지 제도를 볼 수 있어야 한다.

### `POST /api/v1/chat/sessions`

- 기능: AI 서버에 챗봇 세션을 생성한다.
- 사용 위치: 2단계 챗봇, 맞춤 제도 화면의 상담 챗봇.
- 연결 이유: 2단계 진단과 상황 업데이트 상담 모두 AI 서버에 자연어 입력을 보내려면 세션 키가 필요하다.

### `POST /api/v1/chat/sessions/{session_id}/messages`

- 기능: 사용자의 자연어 메시지를 AI 서버에 전송한다.
- 사용 위치: 2단계 챗봇, 맞춤 제도 화면의 상담 챗봇.
- 연결 이유: AI 서버가 사용자의 상황을 파싱해 DB 상태를 업데이트하는 핵심 입력 경로다.

### `POST /api/v1/chat/sessions/{session_id}/match`

- 기능: AI 서버가 현재 수집한 상태를 바탕으로 맞춤 제도 매칭을 실행한다.
- 사용 위치: 2단계 챗봇 완료 시, 상담 챗봇에서 `ready_to_match`가 반환될 때.
- 연결 이유: 최초 맞춤 제도 생성과 상담 후 상황 변경에 따른 재매칭에 필요하다.

### `GET /api/web/policies/matched`

- 기능: 로그인한 사용자의 맞춤 지원 제도 목록을 유형별로 조회한다.
- 사용 위치: 맞춤 제도 화면, 상담 챗봇에서 상황 업데이트/재매칭 후 새로고침.
- 연결 이유: AI 서버가 만든 매칭 결과를 실제 카드 UI로 표시하는 기준 API다.

### `GET /api/web/policies/{policy_id}`

- 기능: 특정 제도의 상세 정보를 조회한다.
- 사용 위치: 제도 카드의 자세히 보기.
- 연결 이유: 신청 방법, 필요 서류, 지원 기간, 공식 링크 등 상세 화면 데이터를 채운다.

### `POST /api/v1/policies/{policy_id}/translate`

- 기능: 특정 제도를 쉬운 말로 풀어쓴 설명을 반환한다.
- 사용 위치: 제도 상세 페이지 우측 단방향 챗봇.
- 연결 이유: 사용자가 제도 상세를 읽을 때 바로 이해를 돕는다. 현재 화면 구조상 우측 챗봇 메시지로 자연스럽게 제공할 수 있다.

### `GET /api/web/users/me/saved-policies`

- 기능: 로그인한 사용자가 저장한 제도 목록을 조회한다.
- 사용 위치: 맞춤 제도 화면의 "내가 선택한 제도", 저장/취소 후 새로고침, 상담 후 맞춤 제도 데이터 새로고침.
- 연결 이유: 저장 상태와 저장 제도 섹션을 화면에 반영해야 한다.

### `POST /api/web/users/me/saved-policies`

- 기능: 특정 제도를 저장한다.
- 사용 위치: 제도 카드/상세의 저장 버튼.
- 연결 이유: 사용자가 관심 있는 제도를 저장하고 이후 앱 알림 흐름으로 이어질 수 있게 한다.

### `DELETE /api/web/users/me/saved-policies/{saved_policy_id}`

- 기능: 저장한 제도를 취소한다.
- 사용 위치: 저장된 제도 카드/상세의 저장 취소 액션.
- 연결 이유: 사용자가 더 이상 알림이나 저장 상태를 원하지 않는 제도를 해제할 수 있어야 한다.

## 보류 API

### `GET /api/v1/chat/state`

- 기능: 로그인 유저의 챗봇 진행 상태를 조회한다.
- 보류 이유: 현재 웹에는 챗봇 이어하기 UI가 없다. 2단계 챗봇과 상담 챗봇 모두 진입 시 새 세션을 만들고, 상담 로그 자체는 저장하지 않는 전제로 동작한다.

### `DELETE /api/v1/chat/state`

- 기능: 챗봇 진행 상태를 초기화한다.
- 보류 이유: 현재 웹 기획에는 상담 초기화/진단 초기화 버튼이 없다. 잘못 연결하면 AI 서버가 관리하는 상태를 사용자가 의도치 않게 지울 수 있다.

### `GET /api/web/users/me/diagnosis-profile`

- 기능: 내 2단계 진단 프로필, 돌봄 대상자, 소득 추론 근거를 조회한다.
- 보류 이유: 웹에는 진단 결과를 직접 확인하는 화면이 없다. 사용자의 상황 변경은 상담 챗봇에 자연어로 말하고 AI 서버가 파싱/저장하는 흐름이다.

### `PATCH /api/web/users/me/diagnosis-profile`

- 기능: 내 진단 프로필을 직접 수정한다.
- 보류 이유: 웹에는 진단 프로필 수정 폼이 없다. 직접 폼 수정이 아니라 상담 챗봇을 통해 상황을 업데이트하는 기획이다.

### `POST /api/web/users/me/cared`

- 기능: 돌봄 대상자를 추가한다.
- 보류 이유: 웹에는 돌봄 대상자 직접 관리 화면이 없다. 돌봄 대상자 변화는 상담 챗봇을 통해 AI 서버가 처리한다.

### `PATCH /api/web/users/me/cared/{cared_id}`

- 기능: 돌봄 대상자를 수정한다.
- 보류 이유: 웹에는 돌봄 대상자 직접 수정 화면이 없다. 상담 챗봇 기반 업데이트 흐름과 중복된다.

### `DELETE /api/web/users/me/cared/{cared_id}`

- 기능: 돌봄 대상자를 삭제한다.
- 보류 이유: 웹에는 돌봄 대상자 직접 삭제 화면이 없다. 상담 챗봇 기반 업데이트 흐름과 중복된다.

### `GET /api/web/users/me/income-signals`

- 기능: AI가 자연어에서 추론한 소득 근거를 조회한다.
- 보류 이유: 웹에는 소득 추론 근거 확인 화면이 없다. 소득 변화는 상담 챗봇에 말하고 AI 서버가 구조화해 저장하는 흐름이다.

### `PATCH /api/web/users/me/income-signals/{signal_id}`

- 기능: 소득 추론 충돌을 해결하거나 사용자가 확인한 값을 기록한다.
- 보류 이유: 웹에는 소득 추론 충돌 해결 UI가 없다. 현재 기획상 사용자는 상담 챗봇을 통해 소득 상황을 갱신한다.

### `GET /api/web/users/me/interest-policy-types`

- 기능: 로그인 유저의 관심 제도 유형을 조회한다.
- 보류 이유: 웹에는 가입 후 관심 유형을 직접 확인/수정하는 화면이 없다. 관심 유형 변화는 상담 챗봇을 통해 처리하는 흐름으로 이해한다.

### `PATCH /api/web/users/me/interest-policy-types`

- 기능: 로그인 유저의 관심 제도 유형을 전체 교체한다.
- 보류 이유: 웹에는 관심 유형 직접 수정 화면이 없다. 사용자가 원하는 정책 유형이 바뀌면 상담 챗봇에 자연어로 말하고 AI 서버가 반영하는 방향이다.

### `GET /api/web/policy-types`

- 기능: 제도 유형 목록을 조회한다.
- 보류 이유: 현재 웹의 관심 유형 선택과 카드 분류는 프론트 상수 `SUPPORT_TYPES`로 처리한다. 다만 상담 챗봇으로 유저의 관심 정책 유형이 바뀔 수 있으므로, 향후 서버 기준 유형과 프론트 상수를 동기화해야 하면 이 API를 동적 옵션 소스로 연결할 수 있다.

### `GET /api/web/agencies`

- 기능: 기관 목록을 조회한다.
- 보류 이유: 웹에는 기관 필터나 기관 목록 화면이 없다.

### `GET /api/web/agencies/{agency_id}`

- 기능: 기관 상세와 연결된 제도 목록을 조회한다.
- 보류 이유: 웹에는 기관 상세 화면이 없다.

### `GET /api/web/documents`

- 기능: 서류 목록과 발급처를 조회한다.
- 보류 이유: 웹의 서류 UX는 제도 상세의 필요서류 버튼에서 간단한 발급 방법과 링크를 제공하는 수준이다. 서류 마스터 선택 UI는 없다.

### `GET /api/web/documents/{document_id}`

- 기능: 서류 상세, 발급처, 연결 제도 목록을 조회한다.
- 보류 이유: 웹에는 서류 상세 화면이 없다. 현재는 제도 상세 카드 내부의 간단 안내로 충분하다.

### `GET /api/web/users/me/document-history`

- 기능: 사용자의 서류 이력을 조회한다.
- 보류 이유: 체크리스트와 서류 준비 상태 관리는 모바일 기능이다. 웹에서는 서류 이력을 직접 보여주지 않는다.

### `POST /api/web/users/me/document-history`

- 기능: 서류 이력을 저장한다.
- 보류 이유: 웹에서는 서류 체크리스트/보유 이력 저장을 제공하지 않는다. 모바일 기능으로 본다.

### `PATCH /api/web/users/me/document-history/{history_id}`

- 기능: 서류 이력을 수정한다.
- 보류 이유: 웹에서는 서류 체크리스트/보유 이력 수정을 제공하지 않는다. 모바일 기능으로 본다.

### `DELETE /api/web/users/me/document-history/{history_id}`

- 기능: 서류 이력을 삭제한다.
- 보류 이유: 웹에서는 서류 체크리스트/보유 이력 삭제를 제공하지 않는다. 모바일 기능으로 본다.

### `GET /api/web/policies`

- 기능: 전체 제도 목록을 검색/필터링한다.
- 보류 이유: 현재 웹의 핵심 화면은 맞춤 제도 추천이다. 전체 제도 탐색을 맞춤 화면에 노출하면 UX 목적이 흐려진다. 또한 현재 서버에서 500이 발생한 이력이 있어 백엔드 안정화 전에는 직접 연결하지 않는다.

### `GET /api/v1/policies?ids=...`

- 기능: 제도 ID 목록으로 AI 서버에서 카드 정보를 조회한다.
- 보류 이유: 현재 맞춤 제도 화면은 AI 매칭 후 Spring 서버의 `/api/web/policies/matched`를 조회해 표시한다. 별도 즉시 카드 렌더링이 필요할 때만 연결한다.

### `PATCH /api/web/users/me/matched-policies/{matched_policy_id}`

- 기능: 추천된 제도의 수혜 여부를 수정한다.
- 보류 이유: 웹에는 "이미 이용한 제도" 표시/필터 UX가 없다. 이 기능은 추천 목록에서 어떻게 반영할지 정책이 정해진 뒤 연결해야 한다.
